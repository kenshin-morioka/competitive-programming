// 自分の回答
use proconio::input;
fn main() {
    input! { mut n:u32 }
    let mut arr: Vec<u32> = Vec::new();
    // if n == 1 {
    //     println!("{}", n);
    //     return;
    // }
    // while n > 1 {
    //     arr.insert(0, n % 2);
    //     n /= 2;
    // }
    // arr.insert(0, n);
    for _ in 0..10 {
        arr.insert(0, n % 2);
        n /= 2;
    }
    let result: String = arr.iter().map(|x| x.to_string()).collect();
    println!("{}", result);
}
