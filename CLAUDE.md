# competitive-programming リポジトリの方針

競技プログラミング(AtCoder 等)の学習用リポジトリ。

## 添削の基本フロー

1. ユーザーが問題を解き、回答ファイルを作ってレビューを依頼する
   - ファイルの雛形は `mise run new <問題名>` で生成する (例: `mise run new tessoku-a13-calculate_distance`)
2. Claude はレビューする: 計算量・より良いアルゴリズム・Rust のイディオム等を指摘。改善版コードを回答内で示すのは OK
3. ユーザーが「ファイルを添削形式にして」と依頼したら、Claude は下記フォーマットにファイルを書き換える。**このとき「添削後の回答」のコードは書かない(ユーザーが自分で書く)**
4. ユーザーが自分で正解コードを書き、コミットする

## 添削ファイルのフォーマット

1 問につき最大 3 ファイル。**ディレクトリは切らず、ファイル名のサフィックスで分ける**
(`src/bin/` 直下に置かないと `cargo run --bin <名前>` が動かなくなるため)。

| ファイル | 中身 | 備考 |
| --- | --- | --- |
| `tessoku-a07-event_attendance.rs` | 添削後の回答 | 実行対象。ファイル名は変えない |
| `tessoku-a07-event_attendance-mine.txt` | 自分の回答 | 元のコードをそのまま保存 (絶対に消さない)。`.rs` にすると `cargo build` の対象に入るので `.txt` |
| `tessoku-a07-event_attendance-review.md` | 添削ポイント | Markdown で箇条書き |

`.rs` には添削コメントを書かない。添削は必ず `-review.md` に置く。

添削依頼を受けたときの手順:

1. 元の回答を `-mine.txt` に移す (先頭に `// 自分の回答 (TLE)` のように結果や問題点を一言添える)
2. 添削ポイントを `-review.md` に書く
3. `.rs` は空にする、または `use` だけ残す。**添削後の回答コードは Claude は書かない(ユーザーが自分で書く)**

補足:

- **元の回答が正解(AC かつ計算量も問題なし)の場合は `-mine.txt` を作らない**: `.rs` がそのまま自分の回答なので、`-review.md` を添えるだけの 2 ファイル構成にする。`tessoku-a03` / `tessoku-a05` / `tessoku-a09` が実例
- ユーザーが書いた `memo1:` 等の学習メモコメントも消さずに残す。**配置は付随先で決める**: 元の回答と一緒に書かれていた memo は `-mine.txt` へ、添削後の回答について書かれた memo は `.rs` へ移す。`-review.md` には入れない
- 添削ポイントは「なぜ遅いか」「どう考えれば改善に辿り着くか」を重視して書く
- **添削は初回で完結させる**: アルゴリズム・計算量だけでなく、入力の受け取り方(多重配列 vs タプル等)・型や境界の注意(usize の引き算順序、オーバーフロー)・命名などの Rust イディオムも最初の `-review.md` に全部含める。後から小出しにしない
- **添削時はチャット回答に改善版コードの例も示す**: `.rs` は空のままだが、チャット上には完全な回答例を出す(ユーザーは見ながらではなく理解してから自分で書く)

## その他

- コミットメッセージは既存に倣う (例: `tessoku a07 event attendance`)
- このリポジトリは学習用の個人リポジトリのため、ブランチ作成・PR 作成は不要。main に直接コミットしてよい
