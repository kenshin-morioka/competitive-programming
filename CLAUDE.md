# competitive-programming リポジトリの方針

競技プログラミング(AtCoder 等)の学習用リポジトリ。

## 添削の基本フロー

1. ユーザーが問題を解き、回答ファイル(例: `atcoder/rust/src/bin/tessoku-a07-event_attendance.rs`)を作ってレビューを依頼する
2. Claude はレビューする: 計算量・より良いアルゴリズム・Rust のイディオム等を指摘。改善版コードを回答内で示すのは OK
3. ユーザーが「ファイルを添削形式にして」と依頼したら、Claude は下記フォーマットにファイルを書き換える。**このとき「添削後の回答」のコードは書かない(ユーザーが自分で書く)**
4. ユーザーが自分で正解コードを書き、コミットする

## 添削ファイルのフォーマット

既存の `tessoku-a06-how_many_guests.rs` / `tessoku-a07-event_attendance.rs` が実例。

```rust
// 自分の回答 (ACだが O(N * D))   ← 結果や問題点を一言添える (TLE 等)
// use proconio::input;
// ...元の回答をそのままコメントアウトして保存 (絶対に消さない)...

// 添削:
// - 計算量: 元の回答は O(N * D)。最悪 10^10 回で本来は TLE 級。
// - imos法: 区間 [l, r] への +1 を diff[l] += 1, diff[r+1] -= 1 の記録に置き換える。
// - 復元: diff の累積和がその日の在室人数。全体 O(N + D)。
// - 配列サイズ・型など、実装上の注意点も箇条書きで。

// 添削後の回答
(ここはユーザーが自分で実装する。Claude は空のまま残す)
```

補足:

- ユーザーが書いた `memo1:` 等の学習メモコメントも消さずに残す
- 添削ポイントは「なぜ遅いか」「どう考えれば改善に辿り着くか」を重視して書く
- **添削は初回で完結させる**: アルゴリズム・計算量だけでなく、入力の受け取り方(多重配列 vs タプル等)・型や境界の注意(usize の引き算順序、オーバーフロー)・命名などの Rust イディオムも最初の「添削:」に全部含める。後から小出しにしない
- **添削時はチャット回答に改善版コードの例も示す**: ファイルの「添削後の回答」は空のままだが、チャット上には完全な回答例を出す(ユーザーは見ながらではなく理解してから自分で書く)

## その他

- コミットメッセージは既存に倣う (例: `tessoku a07 event attendance`)
- このリポジトリは学習用の個人リポジトリのため、ブランチ作成・PR 作成は不要。main に直接コミットしてよい
